# Base de datos (Railway proporciona esta variable automáticamente)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# JWT
JWT_SECRET=cambia_esto_por_una_clave_muy_segura
JWT_EXPIRES_IN=8h

# Servidor
PORT=3001
NODE_ENV=development

# Frontend (para CORS)
FRONTEND_URL=http://localhost:5173

# Email (Microsoft 365 / Exchange Online)
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu@mediapro.tv
SMTP_PASS=tu_contrasena
SMTP_FROM=Mediapro Presupuestos <tu@mediapro.tv>
